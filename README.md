# Universe Group - Task Management

## 🚀 Запуск проекту

### Вимоги

- Node.js 18+
- npm або yarn

### Встановлення залежностей

```bash
npm install
```

### Запуск у режимі розробки

```bash
npm run dev
```

Проект буде доступний за адресою [http://localhost:3000]

## 🎨 Технології

- **Next.js 14**
- **TypeScript**
- **Tailwind CSS**
- **Zustand**
- **React Hot Toast**
- **Class Variance Authority**

## 💡 Прийняті рішення та архітектурні вибори

### 1. Структура маршрутів

**Рішення:** Сторінка задач розміщена безпосередньо в `app/page.tsx` замість окремого роута

**Обґрунтування:** Оскільки в проекті лише одна основна сторінка (сторінка управління задачами), створення окремого роута було б надлишковим. Це спрощує структуру проекту та навігацію

### 2. Хедер та сайдбар

**Рішення:** Хедер та сайдбар, які присутні на 2-му та 3-му екранах макету, не були реалізовані

**Обґрунтування:**

- На більшості екранів макету ці елементи відсутні
- В технічному завданні не описана взаємодія з цими елементами
- Відсутність функціональності для них вказує на те, що вони були залишені в макеті випадково

### 3. Відступи та падінги

**Рішення:** Деякі відступи можуть відрізнятися від макету

**Обґрунтування:** В макеті різні елементи в різних місцях мають різні відступи, що створює неконсистентність. Для забезпечення візуальної узгодженості та кращого UX були прийняті рішення щодо уніфікації відступів на власний розсуд, зберігаючи загальну структуру та пропорції макету

### 4. Іконка хрестика в компоненті Chips

**Рішення:** Іконка хрестика в компоненті `Chips` відрізняється від тієї, що в UI kit

**Обґрунтування:** Використана існуюча іконка `Close` зі спільної бібліотеки іконок для підтримки консистентності та зменшення дублювання коду. Плюс іконка яка використовується в самому компоненті має некоректні розміри. Іконка забезпечує ту саму функціональність та візуально відповідає призначенню

(це лише частина таких моментів яки спричинені макетом)

### 5. Архітектура компонентів

**Рішення:** Використання Feature-Sliced Design

### 6. Управління станом

**Рішення:** Використання Zustand для клієнтського стану задач

**Обґрунтування:**

- Легка та мінімалістична бібліотека
- Не потребує провайдерів навколо всього додатку
- Простий API для роботи з асинхронними операціями
- Інтегрується з Server Components через ініціалізатор

### 7. Зберігання даних

**Рішення:** Використання JSON файлу (`data/tasks.json`) для зберігання задач.

### 8. Лоадер та обробка порожнього стану

**Рішення:** Додано лоадер для GET запиту та обробку випадку, коли задач немає

## ⚠️ Відомі обмеження

### 1. Зберігання даних

- Дані зберігаються в JSON файлі
- Немає персистентності між сесіями (при перезавантаженні сервера дані скидаються до початкового стану)
- **GET `/api/tasks` повертає фіксований список з 20 задач** - згідно з ТЗ, GET endpoint завжди повертає mockTasks (фіксований список з 20 задач). Сторінка також показує цей фіксований список. POST/PUT/DELETE працюють з файлом і **зберігають створені/оновлені задачі в JSON файл** (вони залишаються в файлі), але не відображаються на сторінці та в GET endpoint (відображається тільки фіксований набір з 20 задач). **Важливо:** створені/оновлені задачі відображаються в UI після створення (додаються в Zustand store), але після перезавантаження сторінки зникають, хоча вони залишаються в файлі

### 2. Валідація

- Базова валідація форм присутня (перевірка на порожні поля)
- Валідація формату Task Key не використовується в формі (функція `isValidTaskKey` існує, але не застосовується)
- Обмеження довжини полів не перевіряються (Task Name: макс. 200 символів, Task Key: макс. 50 символів, Description: макс. 2000 символів)
- Обмеження кількості лейблів не перевіряється (ліміт 10 лейблів на задачу)
- Відсутня валідація на рівні API для додаткового захисту
- Перевірка унікальності Task Key тільки на сервері (користувач дізнається про конфлікт тільки після відправки форми)

### 3. Обробка помилок

- Реалізована базова обробка помилок
- Відсутня retry логіка для мережевих помилок
- Відсутня обробка timeout при запитах
- Відсутня обробка race conditions при одночасних запитах

### 4. Функціональні обмеження

- Задачі зі статусом "In Progress" не можна видалити. Згідно з ТЗ

### 5. Безпека

- Відсутні обмеження на розмір запиту та кількість запитів
- Відсутня обробка конкурентних записів (можливі race conditions при одночасних записах)
- Відсутні транзакції (відсутня атомарність операцій read → write)

### 6. Продуктивність

- Всі задачі завантажуються одразу (віртуалізація і так далі)
- Відсутнє кешування даних (використовується `cache: 'no-store'`)
- Відсутнє оптимістичне оновлення UI

### 7. Адаптивність

- Додана базова адаптивність
